name: orocos.dfki.toolchain

version_control:
    - .*:
        type: git
        url: $GITORIOUS_ROOT/orocos-dfki-toolchain/$PACKAGE.git

overrides:
    - orogen:
        url: $GITORIOUS_ROOT/orocos-dfki-toolchain/$PACKAGE.git

